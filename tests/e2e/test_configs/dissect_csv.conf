# Dissect for pipe-delimited log lines, CSV for comma-delimited records
# This config uses dissect for the pipe-delimited lines
input {
  stdin { }
}

filter {
  dissect {
    mapping => { "message" => "%{event_time}|%{level}|%{component}|%{detail}" }
  }
}

output {
  stdout { }
}
