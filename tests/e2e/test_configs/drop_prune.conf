# Drop events by condition and prune fields
input {
  stdin { }
}

filter {
  json {
    source => "message"
  }
  if [level] == "DEBUG" {
    drop { }
  }
  prune {
    whitelist_names => ["level", "service", "action", "status"]
  }
}

output {
  stdout { }
}
