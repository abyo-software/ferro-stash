# Truncate — cap a field to a fixed byte length. Common guard against oversized
# fields before indexing; verifies byte-accurate cut points (not char counts).
input {
  stdin { }
}

filter {
  truncate {
    fields => ["message"]
    length_bytes => 16
  }
}

output {
  stdout { codec => "json" }
}
