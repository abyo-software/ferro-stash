# KV — custom field/value separators into a nested target. The amp/equals
# form is the classic query-string / metric-tag shape.
input { stdin { } }
filter {
  kv {
    source => "message"
    field_split => "&"
    value_split => "="
    target => "params"
  }
}
output { stdout { codec => "json" } }
