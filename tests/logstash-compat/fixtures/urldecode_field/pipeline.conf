# urldecode — percent-decode a field in place. Core Logstash filter, ubiquitous
# for web-access logs where query params and paths are URL-encoded.
input { stdin { } }
filter {
  json { source => "message" }
  urldecode { field => "q" }
}
output { stdout { codec => "json" } }
