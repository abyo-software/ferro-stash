# Passthrough — no filters, events flow input → output unchanged.
# Verifies the Logstash DSL parser, pipeline plumbing, and Event JSON round-trip.
input {
  stdin { }
}

output {
  stdout { codec => "json" }
}
