# ferro-stash fuzz artifact triage — 2026-05-07

Branch: `main` @ `fe11785` (clean vs `origin/main`).

## Summary

10 artifacts triaged. **All 10 reproduce clean against HEAD** — every artifact
is a libfuzzer perf-gate emission (`slow-unit-*`, `timeout-*`, `oom-*`) from a
prior fuzz wave whose underlying defect has since been fixed and seeded.
**Zero new findings**. Zero commits required.

The artifacts have been preserved under `fuzz/known-crash/<target>/` with
date suffix `-2026-05-07` for audit trail.

## Reproduction results

| target | artifact | size | rerun result |
| --- | --- | --- | --- |
| cef_decode | slow-unit-abf58f39 | 27B unverified[^1] | 1 ms — clean |
| cef_decode | timeout-12091be9 | 2.7 KB | 5 ms — clean |
| cef_decode | timeout-44683c41 | 2.7 KB | 5 ms — clean |
| netflow_decode | slow-unit-23f67d60 | small | 9 ms — clean |
| netflow_decode | timeout-18ba8ea3 | small | 1 ms — clean |
| netflow_decode | slow-unit-6908ba70 | small | 0 ms — clean |
| netflow_decode | timeout-acbddce7 | 1.1 KB | 4 ms — clean |
| logstash_dsl_parse | timeout-6eb83547 | 141 B | 5 ms — clean |
| codec_decode | slow-unit-5ac4cfe8 | small | 0 ms — clean |
| codec_decode | oom-da39a3ee | **0 B** (empty file) | 0 ms — clean (libfuzzer threshold artifact) |

[^1]: `da39a3ee5e6b4b0d3255bfef95601890afd80709` is the SHA1 of the empty
string; the codec_decode `oom-` artifact with that hash is a 0-byte file
emitted by libfuzzer when an empty seed crossed the rss_limit threshold once
and was hashed on disk. Reruns deterministically empty-path return.

## Classification

All 10 artifacts: **already-fixed (stale)**.

Likely closing commits (chronological):
- `d1103a1 fix(codec,config): three production DoS panics surfaced by 60s fuzz`
- `ea7cb00 fix(codec): avro OCF block_size signed-varint underflow`
- `6934553 fix(codec): EDN parser hot-loop DoS — depth cap + skip structural-tag stringify`
- `35a12cd test(fuzz): promote 5 regression seeds — fix landed, replay clean`
- `162338d fuzz: add focused cef_decode target`

The corresponding regression seeds already live in
`fuzz/corpus/codec_decode/regression-*` and
`fuzz/corpus/logstash_dsl_parse/regression-dsl-char-boundary-2026-05-02`,
covering the protobuf overflow, avro len/offset, EDN hot-loop, and
DSL char-boundary classes.

## Outstanding

- `cef_decode` and `netflow_decode` corpora have **no `regression-*` seeds yet**
  — they are recent scaffolds (`162338d`) and 24 h smoke runs are still
  collecting baseline coverage. Re-evaluate after the next 24 h runner cycle.
- No upstream / dependency findings.
- No unclassified leftovers.

## Audit-trail moves

```
fuzz/artifacts/cef_decode/*               -> fuzz/known-crash/cef_decode/<name>-2026-05-07
fuzz/artifacts/netflow_decode/*           -> fuzz/known-crash/netflow_decode/<name>-2026-05-07
fuzz/artifacts/logstash_dsl_parse/*       -> fuzz/known-crash/logstash_dsl_parse/<name>-2026-05-07
fuzz/artifacts/codec_decode/*             -> fuzz/known-crash/codec_decode/<name>-2026-05-07
```

`fuzz/artifacts/<target>/` directories are now empty (the parent dirs are
left in place for the next libfuzzer run).
